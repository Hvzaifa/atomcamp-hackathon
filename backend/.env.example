# Backend secrets — copy to backend/.env and fill in real values.
# backend/.env is gitignored; NEVER commit a real key.

# OpenAI API key used by all four agents (the backend calls gpt-4o).
OPENAI_API_KEY=sk-your-openai-api-key-here
